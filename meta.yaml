package:
  name: path-and-address
  version: 2.0.1

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 0
  script: pip install "path-and-address==2.0.1"

requirements:
  build:
    - python
  run:
    - python
